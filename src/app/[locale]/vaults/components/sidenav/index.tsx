import DepositForm from './components/DepositForm';

const SideNav = () => {
  return (
    <div className="bg-[#202020] p-3 text-muted-foreground flex flex-col text-sm">
      <DepositForm />

      {/* Positions Data */}
    </div>
  );
};

export default SideNav;
